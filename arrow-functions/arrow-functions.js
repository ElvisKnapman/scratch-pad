// arrow function
const square = (num) => {
    return num * num;
}

// one line return - can omit brackets if just one parameter and omit curly braces and return keyword if it's just a return statement in the function body
const square = num => num * num;